const currentWeather = {
    temperature: 85,
    conditions: "super sunny",
};

console.log("Current weather:", currentWeather);
console.log(`It's ${currentWeather.temperature} degrees F and ${currentWeather.conditions}.`);